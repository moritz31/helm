apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keepalived.fullname" . }}-test-connection"
  labels:
    {{- include "keepalived.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keepalived.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
